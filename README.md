# Parallel-Image-Processing-

Aplikasi Python berbasis SIMD dan Non-SIMD untuk konversi gambar (grayscale + edge detection) secara paralel. Mendukung mode CLI (batch) dan GUI (interaktif).

## 📌 Fitur
- SIMD vs Non-SIMD (Grayscale & Edge Detection)
- GUI interaktif untuk preview hasil dan grafik benchmark
- CLI mode untuk batch processing banyak gambar
- Output otomatis disimpan di folder hasil
